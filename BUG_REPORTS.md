# Баг-репорты OpenClaw Desktop

## Общая информация

- **Дата тестирования:** 2026-03-07
- **Окружение:** WSL2 (Ubuntu 22.04), Node.js v22.22.1, Electron 31.4.0
- **Тестировщик:** QA Tester Subagent

## Приоритеты

- **Critical:** Блокирующая ошибка, приложение не запускается или ключевая функция недоступна.
- **Major:** Основная функция не работает, но есть обходной путь.
- **Minor:** Незначительная проблема, не влияющая на основные функции.
- **Trivial:** Косметическая проблема, опечатки, мелкие UI недочёты.

---

## #1 Отсутствуют модули googleIntegration и githubIntegration

- **ID:** BUG-001
- **Severity:** Critical
- **Priority:** High
- **Environment:** Linux (WSL2)
- **Module:** main.js

**Описание:**
В файле `main.js` происходит импорт модулей:

```javascript
const googleIntegration = require('./googleIntegration');
const githubIntegration = require('./githubIntegration');
```

Однако в директории проекта отсутствуют файлы `googleIntegration.js` и `githubIntegration.js`.

**Последствия:**
При запуске приложения будет выброшена ошибка `Error: Cannot find module './googleIntegration'`, которая приведёт к падению всего приложения. Интеграции с Google и GitHub полностью неработоспособны.

**Шаги воспроизведения:**

1. Установить зависимости (`npm install`).
2. Запустить приложение (`npm start`).
3. Наблюдать ошибку в консоли и крах приложения.

**Expected:**
Модули должны быть доступны (либо как локальные файлы, либо как пакеты в `node_modules`).

**Actual:**
Модули отсутствуют, приложение не запускается.

**Рекомендация:**

- Создать файлы `googleIntegration.js` и `githubIntegration.js` с корректной реализацией.
- Либо изменить импорт на использование пакетов из `node_modules` (например, `require('openclaw-js/google')`).

---

## #2 Необработанные ошибки в промисах (renderer.js)

- **ID:** BUG-002
- **Severity:** Major
- **Priority:** Medium
- **Environment:** Все платформы
- **Module:** renderer.js

**Описание:**
Многие вызовы асинхронных функций (`window.api.*`) не имеют обработки ошибок (`try/catch` или `.catch`). Например:

- Вкладка «Интеграции»: вызовы `window.api.google.authUrl()`, `window.api.github.init()` и т.д.
- Вкладка «Агенты»: `window.api.openclaw.start()`, `window.api.openclaw.stop()`.
- Вкладка «Файлы»: `window.api.fs.readFile()`.

Если промис будет отклонён (например, из‑за отсутствия сети, неверных токенов, недоступности сервиса), ошибка останется необработанной, что может привести к «зависанию» интерфейса, неявному сбою или непредсказуемому поведению.

**Пример кода (renderer.js строка ~480):**

```javascript
const url = await window.api.google.authUrl();
window.open(url);
```

Нет `try/catch`.

**Последствия:**
Пользователь не получит обратной связи при ошибке; интерфейс может перестать реагировать.

**Шаги воспроизведения:**

1. Запустить приложение.
2. Перейти во вкладку «Интеграции».
3. Нажать кнопку «Google Auth», симулировать сбой сети (например, отключить интернет).
4. Ожидать: сообщение об ошибке или индикатор.
5. Фактически: ничего не происходит или интерфейс блокируется.

**Expected:**
Ошибка должна быть перехвачена и показана пользователю (уведомление, алерт, изменение состояния UI).

**Actual:**
Ошибка не обрабатывается.

**Рекомендация:**
Добавить обработку ошибок для всех асинхронных вызовов. Использовать `try/catch` или `.catch`. Показывать пользователю понятные сообщения об ошибках (например, через toast‑уведомления).

---

## #3 Отсутствие индикаторов загрузки для долгих операций

- **ID:** BUG-003
- **Severity:** Minor
- **Priority:** Low
- **Environment:** Все платформы
- **Module:** renderer.js

**Описание:**
При выполнении долгих операций (загрузка логов, чтение больших файлов, анализ PDF, поиск в интернете) не отображаются индикаторы загрузки (спиннеры, прогресс‑бары). Пользователь не знает, работает ли приложение или зависло.

**Примеры:**

- Загрузка логов (кнопка «Load logs»).
- Загрузка списка файлов из Google Drive.
- Выполнение задачи анализа PDF.

**Последствия:**
Плохой пользовательский опыт; пользователь может думать, что приложение сломалось, и пытаться повторно нажать кнопку, что усугубит проблему.

**Шаги воспроизведения:**

1. Запустить приложение.
2. Перейти во вкладку «Логи».
3. Нажать «Load logs» (если логов много).
4. Наблюдать: нет индикатора, что данные загружаются.

**Expected:**
Во время выполнения асинхронной операции должен отображаться индикатор загрузки (например, спиннер рядом с кнопкой или затемнение области).

**Actual:**
Никакой индикации нет.

**Рекомендация:**
Добавить глобальный или локальный механизм индикации загрузки. Например, перед вызовом API показывать спиннер, после завершения (успех/ошибка) — скрывать.

---

## #4 Не реализована фильтрация логов по уровню (error, warn, info)

- **ID:** BUG-004
- **Severity:** Major
- **Priority:** Medium
- **Environment:** Все платформы
- **Module:** renderer.js

**Описание:**
Во вкладке «Логи» присутствуют кнопки фильтрации по уровням («Error», «Warn», «Info»), но их обработчики не реализованы. В `renderer.js` нет кода, который бы фильтровал отображаемые логи по уровню.

**Код (строки ~90-100):**

```javascript
document.getElementById('filter-error').addEventListener('click', () => {
  /* нет реализации */
});
document.getElementById('filter-warn').addEventListener('click', () => {
  /* нет реализации */
});
document.getElementById('filter-info').addEventListener('click', () => {
  /* нет реализации */
});
```

**Последствия:**
Фильтрация не работает; пользователь видит все логи всегда, что затрудняет поиск ошибок.

**Шаги воспроизведения:**

1. Запустить приложение.
2. Перейти во вкладку «Логи».
3. Нажать кнопку «Error» (или «Warn», «Info»).
4. Ожидать: показываются только логи выбранного уровня.
5. Фактически: список логов не меняется.

**Expected:**
При нажатии на кнопку фильтрации должны отображаться только логи соответствующего уровня.

**Actual:**
Фильтрация не работает.

**Рекомендация:**
Реализовать фильтрацию. Хранить исходный массив логов, при нажатии на кнопку фильтровать и перерисовывать список.

---

## #5 Поиск по логам не имеет debouncing

- **ID:** BUG-005
- **Severity:** Minor
- **Priority:** Low
- **Environment:** Все платформы
- **Module:** renderer.js

**Описание:**
Поле поиска в логах (`#log-search`) вызывает обработчик `searchLogs` на каждое нажатие клавиши без задержки (debouncing). При большом количестве логов это может привести к фризам интерфейса.

**Код (строка ~80):**

```javascript
document.getElementById('log-search').addEventListener('input', searchLogs);
```

**Последствия:**
Падение производительности при быстром вводе.

**Шаги воспроизведения:**

1. Запустить приложение.
2. Перейти во вкладку «Логи».
3. Загрузить много логов (если возможно).
4. Быстро вводить текст в поле поиска.
5. Наблюдать: возможные подтормаживания.

**Expected:**
Поиск должен срабатывать с небольшой задержкой (например, 300 мс) после окончания ввода.

**Actual:**
Поиск срабатывает на каждое нажатие клавиши.

**Рекомендация:**
Добавить debouncing с помощью `setTimeout` или использовать lodash.debounce.

---

## #6 Отсутствует обработка ошибок для shell.openExternal

- **ID:** BUG-006
- **Severity:** Minor
- **Priority:** Low
- **Environment:** Все платформы
- **Module:** main.js

**Описание:**
В обработчике `google-auth-url` (строка ~95) используется `shell.openExternal(url)` без обработки возможных ошибок (например, если URL некорректен или нет доступного браузера).

**Код:**

```javascript
shell.openExternal(url);
```

**Последствия:**
Если `openExternal` выбросит исключение, оно не будет обработано, и пользователь не получит уведомления.

**Шаги воспроизведения:**

1. Запустить приложение.
2. Перейти во вкладку «Интеграции».
3. Нажать «Google Auth» (при условии, что интеграция работает).
4. Симулировать отсутствие браузера (или некорректный URL).
5. Ожидать: ошибка должна быть показана.
6. Фактически: ошибка может быть проигнорирована.

**Expected:**
Ошибка `openExternal` должна быть перехвачена и залогирована; пользователю можно показать сообщение.

**Actual:**
Нет обработки.

**Рекомендация:**
Обернуть вызов в `try/catch` и отправлять уведомление в renderer (или выводить в консоль).

---

## #7 Нет проверки на дублирующиеся клики (кнопки запуска/остановки агента)

- **ID:** BUG-007
- **Severity:** Minor
- **Priority:** Low
- **Environment:** Все платформы
- **Module:** renderer.js

**Описание:**
Кнопки запуска и остановки агента (`#start-agent`, `#stop-agent`) не блокируются после нажатия. Если пользователь быстро нажмёт несколько раз, будут отправлены несколько IPC‑сообщений, что может привести к конфликтам (например, попытка запустить уже запущенный агент).

**Последствия:**
Возможны race conditions и непредсказуемое состояние агента.

**Шаги воспроизведения:**

1. Запустить приложение.
2. Перейти во вкладку «Агенты».
3. Быстро нажать кнопку «Start» несколько раз подряд.
4. Ожидать: только одно выполнение операции.
5. Фактически: отправляются несколько запросов.

**Expected:**
Кнопка должна быть disabled на время выполнения операции, чтобы предотвратить повторные клики.

**Actual:**
Кнопка остаётся активной.

**Рекомендация:**
Добавить флаг `isProcessing` и отключать кнопку на время асинхронного вызова.

---

## #8 Отсутствует подтверждение при удалении файла

- **ID:** BUG-008
- **Severity:** Major
- **Priority:** Medium
- **Environment:** Все платформы
- **Module:** renderer.js

**Описание:**
Во вкладке «Файлы» при клике на кнопку удаления (иконка корзины) файл удаляется сразу, без подтверждения. Это может привести к случайной потере данных.

**Код (строка ~140):**

```javascript
deleteBtn.addEventListener('click', async () => {
  await window.api.fs.deleteFile(filePath); // нет подтверждения
  loadFiles(currentDir);
});
```

**Последствия:**
Пользователь может случайно удалить важный файл.

**Шаги воспроизведения:**

1. Запустить приложение.
2. Перейти во вкладку «Файлы».
3. Навести на файл, нажать иконку корзины.
4. Ожидать: диалог подтверждения.
5. Фактически: файл удаляется немедленно.

**Expected:**
Перед удалением должен появляться диалог подтверждения (например, `confirm()` или модальное окно).

**Actual:**
Нет подтверждения.

**Рекомендация:**
Добавить вызов `window.confirm` или кастомное модальное окно.

---

## #9 Не поддерживается перетаскивание файлов (drag‑and‑drop) в редактор

- **ID:** BUG-009
- **Severity:** Minor
- **Priority:** Low
- **Environment:** Все платформы
- **Module:** renderer.js

**Описание:**
В редакторе кода нет возможности перетащить файл из системы для открытия. Это стандартная функциональность для десктопных приложений.

**Последствия:**
Менее удобный workflow; пользователь должен находить файл через вкладку «Файлы».

**Шаги воспроизведения:**

1. Запустить приложение.
2. Перетащить текстовый файл из проводника в область редактора.
3. Ожидать: файл открывается.
4. Фактически: ничего не происходит (или стандартное поведение браузера).

**Expected:**
Поддержка drag‑and‑drop для открытия файлов в редакторе.

**Actual:**
Не реализовано.

**Рекомендация:**
Добавить обработчики событий `dragover` и `drop` на элемент редактора, читать файл и отображать содержимое.

---

## #10 Нет автосохранения в редакторе

- **ID:** BUG-010
- **Severity:** Minor
- **Priority:** Low
- **Environment:** Все платформы
- **Module:** renderer.js

**Описание:**
Изменения в редакторе не сохраняются автоматически. При закрытии вкладки или случайном обновлении страницы пользователь потеряет несохранённые изменения.

**Последствия:**
Потеря данных.

**Шаги воспроизведения:**

1. Запустить приложение.
2. Открыть файл в редакторе.
3. Внести изменения.
4. Закрыть вкладку редактора (или закрыть приложение) без сохранения.
5. Ожидать: предупреждение о несохранённых изменениях или автосохранение.
6. Фактически: изменения теряются.

**Expected:**
Автосохранение через некоторый интервал и/или предупреждение при закрытии с несохранёнными изменениями.

**Actual:**
Нет ни того, ни другого.

**Рекомендация:**
Реализовать механизм автосохранения (например, каждые 30 секунд) и обработчик `beforeunload`.

---

## #11 Неправильный путь к иконкам в UI_PREVIEW.md

- **ID:** BUG-011
- **Severity:** Trivial
- **Priority:** Low
- **Environment:** Все платформы
- **Module:** UI_PREVIEW.md

**Описание:**
В файле `UI_PREVIEW.md` ссылки на иконки используют относительный путь `images/`, но в репозитории нет директории `images` с этими файлами. Это приводит к битым изображениям в документации.

**Пример:**

```markdown
![Chat Tab](images/chat-tab.png)
```

**Последствия:**
Документация выглядит неаккуратно.

**Шаги воспроизведения:**

1. Открыть `UI_PREVIEW.md` в любом markdown‑просмотрщике.
2. Увидеть битые изображения.

**Expected:**
Изображения должны отображаться.

**Actual:**
Изображения отсутствуют.

**Рекомендация:**
Либо создать директорию `images/` и добавить скриншоты, либо удалить ссылки на изображения из документации.

---

## #12 Отсутствует валидация токенов API (интеграции)

- **ID:** BUG-012
- **Severity:** Major
- **Priority:** Medium
- **Environment:** Все платформы
- **Module:** renderer.js / main.js

**Описание:**
При вводе токенов (GitHub, Perplexity API) нет проверки на минимальную длину, формат или немедленная проверка корректности. Пользователь может ввести мусор, и ошибка проявится только при использовании функции.

**Последствия:**
Пользователь не узнает, что токен неверен, пока не попытается выполнить действие, что ухудшает опыт.

**Шаги воспроизведения:**

1. Запустить приложение.
2. Перейти во вкладку «Интеграции».
3. Ввести случайную строку в поле токена GitHub.
4. Нажать «Save».
5. Ожидать: немедленная ошибка валидации.
6. Фактически: токен сохраняется без проверки.

**Expected:**
Валидация токена (например, проверка длины, формата) перед сохранением.

**Actual:**
Валидации нет.

**Рекомендация:**
Добавить базовую валидацию на стороне renderer (длина, наличие символов) и, возможно, лёгкий запрос к API для проверки токена.

---

## #13 Нет поддержки прокси для внешних запросов

- **ID:** BUG-013
- **Severity:** Minor
- **Priority:** Low
- **Environment:** Все платформы
- **Module:** main.js (интеграции)

**Описание:**
Интеграции (Google, GitHub, Perplexity) не используют настройки прокси, которые могут быть необходимы в корпоративных сетях.

**Последствия:**
Приложение не сможет работать за прокси, интеграции будут недоступны.

**Шаги воспроизведения:**

1. Запустить приложение в сети, требующей прокси.
2. Попытаться использовать любую интеграцию.
3. Ожидать: запросы проходят через прокси.
4. Фактически: запросы падают с таймаутом.

**Expected:**
Поддержка настройки прокси (через переменные окружения или UI настроек).

**Actual:**
Прокси не учитывается.

**Рекомендация:**
Добавить конфигурацию прокси в `main.js` для модулей `googleIntegration`, `githubIntegration` и `taskManager`.

---

## #14 Кросс‑платформенные проблемы: пути к openclaw CLI

- **ID:** BUG-014
- **Severity:** Major
- **Priority:** Medium
- **Environment:** Windows, macOS, Linux
- **Module:** main.js

**Описание:**
В коде используется вызов `openclaw` как CLI команда (например, для запуска Gateway). Предполагается, что `openclaw` доступен в PATH. Однако на разных ОС могут быть различия (расширение `.exe` на Windows, разные пути установки). В текущей реализации нет проверки платформы и подбора корректного пути.

**Код (строка ~30):**

```javascript
const { exec } = require('child_process');
const openclawProcess = exec('openclaw gateway start', { ... });
```

**Последствия:**
На Windows команда `openclaw` может не работать (нужно `openclaw.exe`). Gateway может не запуститься.

**Шаги воспроизведения:**

1. Установить OpenClaw Desktop на Windows.
2. Нажать кнопку запуска Gateway.
3. Ожидать: успешный запуск.
4. Фактически: ошибка «openclaw не является внутренней или внешней командой».

**Expected:**
Определять платформу и использовать правильную команду (`openclaw` для Unix, `openclaw.exe` для Windows).

**Actual:**
Используется одна команда для всех платформ.

**Рекомендация:**
Использовать `process.platform` для ветвления, либо искать бинарник в известных местах.

---

## #15 Нет обработки ситуации, когда Gateway уже запущен

- **ID:** BUG-015
- **Severity:** Minor
- **Priority:** Low
- **Environment:** Все платформы
- **Module:** main.js

**Описание:**
При нажатии кнопки «Start Gateway» не проверяется, запущен ли уже Gateway. Может быть запущено несколько экземпляров, что приведёт к конфликту портов.

**Код (строка ~30):**

```javascript
const startOpenClawGateway = () => { ... }
```

Нет проверки на уже запущенный процесс.

**Последствия:**
Конфликт портов, возможный краш Gateway.

**Шаги воспроизведения:**

1. Запустить приложение.
2. Нажать «Start Gateway».
3. Дождаться запуска.
4. Снова нажать «Start Gateway».
5. Ожидать: сообщение, что Gateway уже запущен.
6. Фактически: попытка запуска второго экземпляра.

**Expected:**
Проверять статус Gateway перед запуском; если уже запущен — показывать уведомление.

**Actual:**
Отсутствует проверка.

**Рекомендация:**
В функции `startOpenClawGateway` вызывать `checkGatewayStatus()` и, если Gateway работает, не запускать повторно.

---

## Заключение

Выявлено **15** проблем:

- Critical: 1
- Major: 5
- Minor: 7
- Trivial: 2

Наиболее критичная — отсутствие модулей `googleIntegration.js` и `githubIntegration.js`, что делает приложение неработоспособным. Рекомендуется начать с её исправления.

Все баги задокументированы с шагами воспроизведения, ожидаемым и фактическим поведением, а также рекомендациями по исправлению.
